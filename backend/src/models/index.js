const Token = require('./token.model');
const User = require('./user.model');
const userStatsModel = require('./user_stats.model');
const friendModel = require('./friend.model');
const messageModel = require('./message.model');

module.exports = {
  Token,
  User,
  userStatsModel,
  friendModel,
  messageModel,
};
